<p id="notice"><%= notice %></p>

<h1>Homes index</h1>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>Make</th>
      <th>Year</th>
      <th>Model</th>
      <th>Price</th>
      <th>Discription</th>
      <th>Owner</th>
      <th>Email</th>
      <th>Phone</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @homes.each do |home| %>
      <tr>
        <td><%= home.category %></td>
        <td><%= home.make %></td>
        <td><%= home.year %></td>
        <td><%= home.model %></td>
        <td><%= home.price %></td>
        <td><%= home.discription %></td>
        <td><%= home.owner %></td>
        <td><%= home.email %></td>
        <td><%= home.phone %></td>
        <td><%= link_to 'Show', home %></td>
        <td><%= link_to 'Edit', edit_home_path(home) %></td>
        <td><%= link_to 'Destroy', home, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Home', new_home_path %>
